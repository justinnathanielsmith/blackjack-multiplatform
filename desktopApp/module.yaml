product: jvm/app

settings:
  compose:
    enabled: true

dependencies:
  - ../sharedUI
  - $libs.kotlinx.coroutines.swing
  - $libs.decompose
  - $libs.essenty.lifecycle
  - $libs.kermit
  - $compose.desktop.currentOs
  - $compose.ui
