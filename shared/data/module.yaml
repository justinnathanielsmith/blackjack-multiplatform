product:
  type: lib
  platforms: [jvm, android, iosArm64, iosSimulatorArm64, iosX64, wasmJs]

settings:
  android:
    namespace: io.github.smithjustinn.blackjack.data

dependencies:
  - ../core
  - $libs.room.runtime
  - $libs.sqlite.bundled
  - $libs.datastore.preferences: exported
